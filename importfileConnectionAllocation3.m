function [src,dst,spec,slots_used,core_src,core_dst,cores_used,tfk_slot] = importfileConnectionAllocation3(filename, startRow, endRow)
%IMPORTFILE Import numeric data from a text file as column vectors.
%   [SRC,DST,SPEC,SLOTS_USED,CORE_SRC,CORE_DST,CORES_USED,TFK_SLOT] =
%   IMPORTFILE(FILENAME) Reads data from text file FILENAME for the default
%   selection.
%
%   [SRC,DST,SPEC,SLOTS_USED,CORE_SRC,CORE_DST,CORES_USED,TFK_SLOT] =
%   IMPORTFILE(FILENAME, STARTROW, ENDROW) Reads data from rows STARTROW
%   through ENDROW of text file FILENAME.
%
% Example:
%   [src,dst,spec,slots_used,core_src,core_dst,cores_used,tfk_slot] = importfile('cnklist_heuristic_0_0.0.csv',2, 927);
%
%    See also TEXTSCAN.

% Auto-generated by MATLAB on 2016/12/12 15:39:54

%% Initialize variables.
delimiter = ',';
if nargin<=2
    startRow = 2;
    endRow = inf;
end

%% Format for each line of text:
%   column1: double (%f)
%	column2: double (%f)
%   column3: double (%f)
%	column4: double (%f)
%   column5: double (%f)
%	column6: double (%f)
%   column7: double (%f)
%	column8: double (%f)
% For more information, see the TEXTSCAN documentation.
formatSpec = '%f%f%f%f%f%f%f%f%[^\n\r]';

%% Open the text file.
fileID = fopen(filename,'r');

%% Read columns of data according to the format.
% This call is based on the structure of the file used to generate this
% code. If an error occurs for a different file, try regenerating the code
% from the Import Tool.
dataArray = textscan(fileID, formatSpec, endRow(1)-startRow(1)+1, 'Delimiter', delimiter, 'HeaderLines', startRow(1)-1, 'ReturnOnError', false, 'EndOfLine', '\r\n');
for block=2:length(startRow)
    frewind(fileID);
    dataArrayBlock = textscan(fileID, formatSpec, endRow(block)-startRow(block)+1, 'Delimiter', delimiter, 'HeaderLines', startRow(block)-1, 'ReturnOnError', false, 'EndOfLine', '\r\n');
    for col=1:length(dataArray)
        dataArray{col} = [dataArray{col};dataArrayBlock{col}];
    end
end

%% Close the text file.
fclose(fileID);

%% Post processing for unimportable data.
% No unimportable data rules were applied during the import, so no post
% processing code is included. To generate code which works for
% unimportable data, select unimportable cells in a file and regenerate the
% script.

%% Allocate imported array to column variable names
src = dataArray{:, 1};
dst = dataArray{:, 2};
spec = dataArray{:, 3};
slots_used = dataArray{:, 4};
core_src = dataArray{:, 5};
core_dst = dataArray{:, 6};
cores_used = dataArray{:, 7};
tfk_slot = dataArray{:, 8};


